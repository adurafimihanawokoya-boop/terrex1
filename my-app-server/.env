# --- MySQL connection (matches schema.sql) ---
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=changeme
DB_NAME=terrex

# --- Server ---
PORT=3000

# --- Admin ---
# Password checked by POST /api/admin/login. Pick something strong.
ADMIN_PASSWORD=adura123
