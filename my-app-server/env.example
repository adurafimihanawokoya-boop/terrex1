# Server
PORT=3000

# MySQL connection (matches schema.sql)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=terrex

# Admin login — POST /api/admin/login with this password
# returns a token that unlocks POST/PUT/DELETE on every table.
ADMIN_PASSWORD=change-me

# Email (Nodemailer) — sends verification codes AND the
# danger-alert / app-update notification emails.
# For Gmail: use an "App Password", not your normal password
# (Google Account > Security > 2-Step Verification > App passwords).
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-address@gmail.com
EMAIL_PASS=your-app-password
EMAIL_FROM=TERREX <your-address@gmail.com>
